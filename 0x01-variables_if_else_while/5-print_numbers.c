#include <stdio.h>
/**
* main - prints all single digit numbers form zero
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int kon;

	for (kon = 0; kon < 10; kon++)
	printf("%d", kon);
	printf("\n");
	return (0);
}
